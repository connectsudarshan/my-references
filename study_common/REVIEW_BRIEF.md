# Independent fact-check brief (NVMe and ZNS study pages)

You are an independent reviewer with strong NVMe / PCIe / SSD firmware knowledge. The content files were written by other authors from memory plus public sources (Linux nvme.h, SPDK, nvme-cli, zonedstorage.io); the spec PDFs themselves were mostly not reachable. The reader is Sudarshan K, preparing for a Principal SSD Firmware Validation interview: a wrong number, opcode, bit position, field name, status code or rule is costly. Your job is to FIND AND FIX factual errors in your assigned modules.

## What to do
1. Read `<topic>/AUTHORING.md` (format and rules) then every question of your assigned modules, carefully, one by one.
2. For each factual claim that could be wrong (opcodes, feature IDs, log page IDs, CNS values, register offsets and bit positions, field widths and units, status code values and names, state-machine rules, rules of thumb, arithmetic, tool names/options/behaviors), check it against a reliable source: Linux `include/linux/nvme.h` (raw.githubusercontent.com/torvalds/linux/master/include/linux/nvme.h), `drivers/nvme/host/*.c`, SPDK `include/spdk/nvme_spec.h`, libnvme/nvme-cli source and docs, `zonedstorage.io`, kernel docs, fio HOWTO, the NVMe specification text where you can reach it. Re-derive EVERY calculation with python. Also check internal consistency inside each question (SHORT vs DEEP vs FOLLOW vs PITFALL) and against sibling modules (`grep` the other content files for the same fact; if two modules disagree, decide which is right from a primary source and fix the wrong one only if it is in your module, otherwise report it).
3. Fix errors IN PLACE in your assigned content files with minimal edits (keep format, ids, levels, types, fig keys unchanged; keep SHORT <= 500 chars). If a claim cannot be verified and is stated too confidently, soften the wording and make sure a VERIFY: line names it. If a VERIFY line is now unnecessary because you confirmed the fact, leave it unless it is clearly wrong. Do not rewrite for style. Do not add em dashes. Do not touch files outside your assigned modules.
4. After edits run `python3 /home/claude/study/build.py /home/claude/<topic> --check /home/claude/<topic>/content/mNN.txt ...` for each of your files and make sure there are no ERRORs.
5. Reply with a report under 250 words: per module, the list of fixes made (question id, what was wrong, what it is now, source), plus any cross-module inconsistencies you could not fix, and claims you could not verify.

Be skeptical of confident-sounding details. Prioritise: numbers and arithmetic, bit/byte positions, opcode and code values, "always/never" rules, and anything that a validation engineer would assert in a test.
